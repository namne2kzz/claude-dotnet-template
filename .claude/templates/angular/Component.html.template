<!-- {{ComponentName}} Component Template -->
<!-- Angular 20: use @if / @for / @switch / @defer — NOT *ngIf / *ngFor -->

@if (isLoading()) {
  <div class="loading-skeleton">
    <!-- TODO: Skeleton loader -->
    Loading...
  </div>
} @else if (error()) {
  <div class="error-message">
    {{ error() }}
  </div>
} @else {
  <!-- Main content -->

  <!-- Example: list rendering -->
  <!--
  @for (item of items(); track item.id) {
    <div class="item-card">
      {{ item.name }}
    </div>
  } @empty {
    <p class="empty-state">No items found</p>
  }
  -->

  <!-- Example: deferred heavy section -->
  <!--
  @defer (on viewport; prefetch on idle) {
    <app-heavy-section [data]="data()" />
  } @placeholder {
    <div class="placeholder">Loading section...</div>
  }
  -->
}
